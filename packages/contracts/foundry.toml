[profile.default]
src = 'src'
out = 'out'
libs = ['lib']

[rpc_endpoints]
georli = "${GOERLI_RPC_URL}"

[etherscan]
georli = { key = "${ETHERSCAN_KEY}" }
# See more config options https://github.com/foundry-rs/foundry/tree/master/config